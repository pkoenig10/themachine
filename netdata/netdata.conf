[plugin:proc:/proc/net/dev]
  disable by default interfaces matching = lo docker* br* veth*

[plugin:proc:/proc/diskstats]
  performance metrics for physical disks = yes
  performance metrics for virtual disks = no
  performance metrics for partitions = no

[plugin:proc:diskspace]
  exclude space metrics on paths = /dev/* /etc/* /host/* /proc/* /sys/* /var/*
  exclude space metrics on filesystems = tmpfs
  space usage for all disks = yes
  inodes usage for all disks = yes
